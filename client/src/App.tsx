import ChatPanel from './components/ChatPanel';

const App = () => {
  return (
    <main className="h-screen w-screen flex justify-center">
      <ChatPanel />
    </main>
  );
};

export default App;
